import styles from './NotEnoughData.module.css'
import React from 'react'

const NotEnoughData = () => (
  <div className={styles.NotEnoughData}>
    <div className={styles.Text}>
      Need more data to render chart.
    </div>
  </div>
)

export default NotEnoughData
